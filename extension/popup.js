/** Change to your deployed origin (no trailing slash). */
const ORIGIN = "https://your-domain.com";

document.getElementById("home").href = `${ORIGIN}/`;
document.getElementById("compress").href = `${ORIGIN}/tools/compress`;
document.getElementById("batch").href = `${ORIGIN}/tools/batch`;
